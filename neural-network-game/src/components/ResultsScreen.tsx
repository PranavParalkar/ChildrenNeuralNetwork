'use client';

import { motion } from 'framer-motion';

interface ResultsScreenProps {
  sentences: string[];
  imageUrl: string;
}

export default function ResultsScreen({ sentences, imageUrl }: ResultsScreenProps) {
  return (
    <div className="min-h-screen bg-gray-950 flex items-center justify-center p-4 sm:p-6">
      <div className="w-full max-w-6xl grid grid-cols-1 lg:grid-cols-2 gap-6 sm:gap-8 items-center">
        {/* Left side - Top 5 sentences */}
        <motion.div
          initial={{ x: -50, opacity: 0 }}
          animate={{ x: 0, opacity: 1 }}
          transition={{ delay: 0.3 }}
        >
          <h2 className="text-2xl sm:text-3xl font-bold text-white mb-1 sm:mb-2">Neural Network Output</h2>
          <p className="text-sm sm:text-base text-gray-400 mb-4 sm:mb-6">Top {sentences.length} sentences generated by the crowd</p>

          <div className="space-y-3 sm:space-y-4">
            {sentences.map((sentence, index) => (
              <motion.div
                key={index}
                initial={{ x: -30, opacity: 0 }}
                animate={{ x: 0, opacity: 1 }}
                transition={{ delay: 0.5 + index * 0.2 }}
                className="relative"
              >
                <div className="flex items-start gap-3 sm:gap-4 p-3 sm:p-4 bg-gray-800/60 rounded-xl border border-gray-700">
                  <div className={`flex-shrink-0 w-7 h-7 sm:w-8 sm:h-8 rounded-full flex items-center justify-center font-bold text-xs sm:text-sm ${
                    index === 0 ? 'bg-yellow-500 text-black' :
                    index === 1 ? 'bg-gray-300 text-black' :
                    index === 2 ? 'bg-amber-700 text-white' :
                    'bg-gray-600 text-gray-200'
                  }`}>
                    {index + 1}
                  </div>
                  <p className="text-gray-200 text-base sm:text-lg leading-relaxed capitalize">{sentence}</p>
                </div>
              </motion.div>
            ))}
          </div>

          {sentences.length === 0 && (
            <div className="text-center py-8 sm:py-12 text-gray-500">
              <p className="text-lg sm:text-xl">No sentences were generated</p>
              <p className="text-xs sm:text-sm mt-2">Not enough players submitted responses</p>
            </div>
          )}
        </motion.div>

        {/* Right side - Original image */}
        <motion.div
          initial={{ x: 50, opacity: 0 }}
          animate={{ x: 0, opacity: 1 }}
          transition={{ delay: 0.5 }}
          className="flex flex-col items-center"
        >
          <h3 className="text-lg sm:text-xl font-semibold text-gray-300 mb-3 sm:mb-4">Original Image</h3>
          <div className="rounded-2xl overflow-hidden border-2 border-cyan-500/30 shadow-2xl shadow-cyan-500/10 w-full max-w-md">
            <img
              src={imageUrl}
              alt="Original image"
              className="w-full h-56 sm:h-80 object-cover"
            />
          </div>

          <motion.div
            className="mt-4 sm:mt-6 text-center"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 1.5 }}
          >
            <p className="text-xs sm:text-sm text-gray-500">
              The crowd processed this image through three neural network layers
            </p>
            <div className="flex items-center justify-center gap-2 sm:gap-4 mt-2 sm:mt-3 flex-wrap">
              <span className="text-blue-400 text-[10px] sm:text-xs">Input → Words</span>
              <span className="text-gray-600">→</span>
              <span className="text-purple-400 text-[10px] sm:text-xs">Hidden → Phrases</span>
              <span className="text-gray-600">→</span>
              <span className="text-amber-400 text-[10px] sm:text-xs">Output → Sentences</span>
            </div>
          </motion.div>
        </motion.div>
      </div>
    </div>
  );
}
